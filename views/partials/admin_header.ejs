<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Admin Panel</title>
  <link rel="stylesheet" href="/public/admin.css" />
</head>
<body>
  <header>
    <div>
      <strong>Admin Panel</strong>
    </div>
    <div>
      <span><%= user.email %> (<%= user.role %>)</span>
      <a href="/logout">Logout</a>
    </div>
  </header>
  <div class="container">
    <% if (warnings && warnings.length) { %>
      <div class="banner">
        <strong>System warnings:</strong> <%= warnings.join(', ') %>
      </div>
    <% } %>
    <nav style="margin-bottom:12px;">
      <a href="/admin" class="<%= (active==='/admin')?'active':'' %>">Dashboard</a>
      <a href="/admin/users" class="<%= (active && active.startsWith('/admin/users'))?'active':'' %>">Users</a>
      <a href="/admin/jobs" class="<%= (active && active.startsWith('/admin/jobs'))?'active':'' %>">Jobs Monitor</a>
    </nav>

{
  "name": "bg-removal-worker",
  "version": "1.0.0",
  "description": "Background removal provider integration with external API and S3, with tests",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.645.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
